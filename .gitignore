node_modules/
dist/
.DS_Store
.superpowers/

# Local secrets and config — never commit. Rhumb reads all credentials
# (CLAUDE_CODE_OAUTH_TOKEN, RHUMB_PROXMOX_*, RHUMB_PG_ADMIN, deploy keys) from
# the environment; keep them out of the repo.
.env
.env.*
!.env.example
